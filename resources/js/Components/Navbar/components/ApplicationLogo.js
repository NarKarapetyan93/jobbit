import React from 'react';

export default function ApplicationLogo({ className }) {
    return (
        <img className={className} src="/images/logo.png" alt="Jobbit logo" title="Jobbit" />
    );
}
